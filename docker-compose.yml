version: "3.9"

services:
  web:
    build: .
    ports:
      - "${APP_PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./data/uploads:/app/public/uploads
    restart: unless-stopped
